# CIStamp Receipt

- **Status:** PASS
- **Created:** `example output generated during smoke runs`
- **Tool:** cistamp@0.1.0
- **Redaction:** enabled

Use `npm run smoke` to generate a fresh local receipt under `.cistamp/smoke/`.
This checked-in sample is intentionally static so the repository stays clean.
